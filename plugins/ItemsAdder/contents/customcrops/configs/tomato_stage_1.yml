info:
  namespace: customcrops
items:
  tomato_seeds:
    enabled: true
    display_name: "Tomato Seeds"
    lore:
      - "&7&oA juicy red fruit, perfect for salads and sauces."
    resource:
      material: PAPER
      generate: true
      textures:
        - item/tomato/tomato_seeds # Link to ItemsAdder/contents/textures/item/tomato/{filename}.png
        
  tomato_stage_1:
    enabled: true
    display_name: tomato_stage_1
    resource:
      material: PAPER
      generate: false
      model_path: item/tomato/tomato_stage_1 # Link to ItemsAdder/contents/textures/item/tomato/{filename}.png
    specific_properties:
      block:
        placed_model:
          type: REAL_WIRE
        cancel_drop: true

  tomato_stage_2:
    enabled: true
    display_name: tomato_stage_2
    resource:
      material: PAPER
      generate: false
      model_path: item/tomato/tomato_stage_2 # Link to ItemsAdder/contents/textures/item/tomato/{filename}.png
    specific_properties:
      block:
        placed_model:
          type: REAL_WIRE
        cancel_drop: true

  tomato_stage_3:
    enabled: true
    display_name: tomato_stage_3
    resource:
      material: PAPER
      generate: false
      model_path: item/tomato/tomato_stage_3 # Link to ItemsAdder/contents/textures/item/tomato/{filename}.png
    specific_properties:
      block:
        placed_model:
          type: REAL_WIRE
        cancel_drop: true

  tomato_stage_4:
    enabled: true
    display_name: tomato_stage_4
    resource:
      material: PAPER
      generate: false
      model_path: item/tomato/tomato_stage_4 # Link to ItemsAdder/contents/textures/item/tomato/{filename}.png
    specific_properties:
      block:
        placed_model:
          type: REAL_WIRE
        cancel_drop: true